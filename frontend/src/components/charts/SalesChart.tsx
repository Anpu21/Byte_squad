/**
 * SalesChart placeholder component.
 * Will be implemented with Recharts during the dashboard phase.
 */
export default function SalesChart() {
    return (
        <div className="h-64 flex items-center justify-center text-[var(--color-text-muted)] text-sm">
            Sales chart will be rendered here using Recharts
        </div>
    );
}
