// Accounting feature — the unified accounting workspace hub (route entry) that
// composes the ledger / receivables / reports / expenses / P&L tabs.
export { AccountingPage } from './AccountingPage';
