// POS feature — the cashier billing screen (route entry). Internal billing-grid,
// payment, shift, loyalty, and print sub-components are composed by PosPage and
// consumed intra-feature; the barrel exposes only the routable screen.
export { PosPage } from './PosPage';
