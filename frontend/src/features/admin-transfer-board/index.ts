// Admin transfer board feature — the admin approvals/history/report workspace
// (route entry). Absorbed the former admin-transfers tab hook.
export { AdminTransfersPage } from './AdminTransfersPage';
