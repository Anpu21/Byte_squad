// HR workspace — the unified Admin/Manager people hub (employees, attendance,
// leaves, payroll tabs). Public surface for the route registry + redirects.
export { AdminHrPage } from './AdminHrPage';
export { useAdminHrTab, type AdminHrTab } from './hooks/useAdminHrTab';
