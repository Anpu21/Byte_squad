// Admin employees feature — the HR roster view, the shared employees query
// (reused by leaves/payroll/attendance views), and the employee CRUD hooks
// (reused by the employee-form feature).
export { EmployeesView } from './components/EmployeesView';
export { useEmployees } from './hooks/useEmployees';
export { useEmployee } from './hooks/useEmployee';
export { useCreateEmployee } from './hooks/useCreateEmployee';
export { useUpdateEmployee } from './hooks/useUpdateEmployee';
export { useUploadEmployeePhoto } from './hooks/useUploadEmployeePhoto';
