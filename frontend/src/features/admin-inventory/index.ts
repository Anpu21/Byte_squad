// Admin inventory feature — the inventory workspace hub (route entry) and the
// admin inventory view used by the role-aware inventory entry.
export { InventoryWorkspacePage } from './InventoryWorkspacePage';
export { AdminInventoryPage } from './AdminInventoryPage';
