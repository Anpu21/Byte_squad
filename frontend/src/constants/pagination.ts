/**
 * Default number of rows shown per page across the app's data tables.
 * This is the single source of truth — change it here to restyle every pager
 * (client-side `usePagination` and the server hooks that send it as `limit`).
 */
export const DEFAULT_PAGE_SIZE = 10;
