{
  "name": "byte_squad-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Root tooling: Husky + lint-staged. Application code lives in frontend/ and backend/.",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "prepare": "husky",
    "verify": "pnpm --dir frontend run verify && pnpm --dir backend run verify"
  },
  "devDependencies": {
    "husky": "9.1.7",
    "lint-staged": "15.2.10"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx}": [
      "bash -c 'cd frontend && pnpm exec eslint --fix \"$@\"' --"
    ],
    "backend/**/*.ts": [
      "bash -c 'cd backend && pnpm exec eslint --fix \"$@\"' --"
    ]
  }
}
