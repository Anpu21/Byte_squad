version: '3.8'

services:
  # LedgerPro Backend API
  backend:
    build:
      context: ./smartbiz-backend
      dockerfile: Dockerfile
    container_name: ledgerpro-backend
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_PATH=/app/data/ledgerpro.db
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRY=24h
      - JWT_REFRESH_EXPIRY=7d
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - BACKUP_KEY=${BACKUP_KEY}
    restart: unless-stopped
    networks:
      - ledgerpro-network

  # PostgreSQL for cloud backup (optional)
  postgres:
    image: postgres:15-alpine
    container_name: ledgerpro-postgres
    environment:
      - POSTGRES_DB=ledgerpro_backup
      - POSTGRES_USER=${PG_USER:-ledgerpro}
      - POSTGRES_PASSWORD=${PG_PASSWORD:-changeme}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    networks:
      - ledgerpro-network

networks:
  ledgerpro-network:
    driver: bridge

volumes:
  postgres_data:
