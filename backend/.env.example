# ── Server ──
PORT=4000

# ── JWT ──
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h

# ── Database (SQLite for local, change to postgres for remote sync) ──
DB_TYPE=better-sqlite3
DB_DATABASE=data.sqlite

# ── PostgreSQL (for future remote sync/backup) ──
# DB_TYPE=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=postgres
# DB_PASSWORD=your_password_here
# DB_DATABASE=ledgerpro
